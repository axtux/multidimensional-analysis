Data directory is accessed at "../data" relative to script directory. You do not need to change directory before loading script. So you can place script into :
- a directory which is in the same directory as the data directory
- in the data direcotry
